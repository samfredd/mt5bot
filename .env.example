# No application settings belong in this file.
#
# Configure trading, MT5 bridge, AI providers, news, Telegram, Twilio, and
# web-search from the authenticated Settings screen. Production bootstrap
# secrets (database connectivity, JWT signing, and encryption key) must be
# supplied by your deployment secret manager; see docs/DEPLOYMENT.md.
